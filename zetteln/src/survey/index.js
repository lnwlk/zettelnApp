// Main export file for zetteln survey components (Tailwind version)
import Survey from './Survey';
import './i18n'; // Initialize i18n

// Export the main Survey component
export default Survey;

// Export other components if needed individually
export { Survey };
